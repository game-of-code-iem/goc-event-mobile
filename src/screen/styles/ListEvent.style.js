import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
    topBarIcon: {
        paddingRight: 15,
        display: "flex",
        flexDirection: "row",
        alignItems: "center"
    },
    floatingButton: {
        marginBottom: 25
    }
})
